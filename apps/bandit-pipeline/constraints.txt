# Retained for local uv workspace overrides — see pyproject.toml tool.uv.override-dependencies.
# pandas<3 required by mlflow; scikit-learn>=1.0 for manylinux wheels.
pandas>=2.0,<3
scikit-learn>=1.0
