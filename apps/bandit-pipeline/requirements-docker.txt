# Docker image deps — obp excluded (imports are try/except with synthetic fallback).
# For local dev with real OBP data: uv sync --extra pipeline
dagster>=1.8
dagster-postgres>=0.24

redis>=5.0
numpy>=1.26
scipy>=1.13
mlflow>=2.14
boto3>=1.34
sqlalchemy>=2.0
psycopg2-binary>=2.9
pyarrow>=16.0
httpx>=0.27
